- if @tags
  #tags
    %h2 Tags
    
    %ul
      - @tags.each do |tag|
        %li
          %a{:href => "/#{@category}/tags/#{tag}"}= tag
          -# = "category: #{#@category}"
          -# %a{:href => "#{(@category == "*" ? "/#{@category}" : "/")}/tags/#{tag}"}= tag